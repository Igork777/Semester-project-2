package client;


import javafx.application.Application;

/**
 * ... The class where the application starts
 */
public class RunBondInBand {
    /**
     * The method, which launches the application
     * @param args array of strings, which stores
     * passed by command line while starting the program
     */
    public static void main(String[] args)
    {
        Application.launch(BondInBandApp.class);
    }
}
